alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd ~/authentication-and-authorization/authentication
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd authentication-and-authorization/authentication
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd authentication-and-authorization/authentication
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd authentication-and-authorization/authentication
cd ~/storage/statefulsets
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd storage/statefulsets
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd storage/statefulsets
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd storage/statefulsets
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd storage/statefulsets
cd ~/application-packaging-with-helm/helm
vi demo/values.yaml 
helm list
kubectl get pods
cd ~/service-mesh-overview/istio
cd ~/flipkart-cloud-platform/fcp-overview
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd service-mesh-overview/istio
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd service-mesh-overview/istio
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd service-mesh-overview/istio
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd service-mesh-overview/istio
pwd
cd ~
find . -type f | xargs grep namespace
find . -type f | xargs grep -i "kind" | grep -i namespace
find . -type f -name "*.yaml" | xargs grep -i "kind" | grep -i namespace
cd core-concepts/namespaces/
ls -l
cat pod.yaml 
cd ~
find . -type f -name "*.yaml" | xargs grep -i "liveness"
find . -type f -name "*.yaml" | xargs grep -i "liveness"cd ~/application-lifecycle-management/health-checks
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd application-lifecycle-management/health-checks
cd ~/application-lifecycle-management/batch-operations-with-job-and-cronjob
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd application-lifecycle-management/batch-operations-with-job-and-cronjob
cd ~/storage/static-volume-management
cd ~/storage/dynamic-volume-provisioning
cd ~/storage/static-volume-management
cd ~
find . -type f -name "*.yaml" | xargs grep NodePort
cat ./services-and-networking/kubernetes-services/nodeport.yaml
alias k=kubectl; tar -xf *.tar.gz 2>/dev/null; tar -xf *.tar 2>/dev/null; unzip -o *.zip 2>/dev/null; source <(kubectl completion bash); mkdir -p ~/.vim ; echo "set tabstop=2" > ~/.vim/vimrc; cd ~/; clear
PROMPT_DIRTRIM=1
cd storage/static-volume-management
cd ~/services-and-networking/kubernetes-services
cd ~
find . -type f -name "*.yaml"| xargs grep ConfigMap 
cat configmaps-secrets/configmaps/config.yaml 
cat storage/statefulsets/mongo-stateful.yaml 
ls -l
cd multi-container-pods/
ls -l
cd ~
git
git init
git add .
git commit -m "All Examples"
git remote add origin https://github.com/Shubham96goyal/k8s.git
git push origin head
git branch
git checkout -b master
ls -l
git add .
git commit -m "All Examples"
git push origin head
git branch -M main
git push -u origin master
